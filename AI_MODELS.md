# 🧠 نماذج الذكاء الاصطناعي في PowerStep

المشروع يحتوي على **4 نماذج** تعمل معاً في [`realtime_inference.py`](realtime_inference.py)

---

## 1. 🦶 `piezo_step_model.joblib`

| | |
|---|---|
| **الاسم** | نموذج كشف الخطوة |
| **النوع** | Random Forest Classifier |
| **المدخلات** | `Voltage (V)` + `Power (W)` من حساس البيزو |

**بيعمل إيه؟**

يأخذ القراءة القادمة من الحساس ويجيب على سؤال واحد فقط:

> **"الاهتزاز ده خطوة حقيقية ولا مجرد تشويش (Noise)؟"**

- لو `voltage > 0.01` والنموذج وافق → يحكم بـ **Step Detected ✅**
- لو تشويش عشوائي من الهواء أو اهتزاز صغير → يحكم بـ **No Step ❌**

---

## 2. 👥 `wifi_occupancy_model.joblib`

| | |
|---|---|
| **الاسم** | نموذج كشف الوجود البشري |
| **النوع** | Random Forest Classifier |
| **المدخلات** | `csi_variance` (تذبذب إشارة الواي فاي) |

**بيعمل إيه؟**

إشارة الواي فاي بتتأثر لما حد يمشي في المنطقة. النموذج بيقيس مدى تذبذب الإشارة ويقرر:

> **"في ناس في المكان ده دلوقتي ولا لأ؟"**

- تذبذب عالي → **Occupied ✅** (في حد)
- تذبذب منخفض → **Empty ❌**

---

## 3. 🔢 `wifi_count_model.joblib`

| | |
|---|---|
| **الاسم** | نموذج تقدير عدد الأشخاص |
| **النوع** | Random Forest Regressor |
| **المدخلات** | `device_count_raw` (عدد الأجهزة الملتقطة من الـ WiFi Sniffing) |

**بيعمل إيه؟**

بعد ما النموذج التاني يقول "في ناس"، النموذج ده بيحاول يعد كام واحد بالظبط:

> **"عدد الأجهزة الملتقطة = كام شخص تقريباً؟"**

- مثلاً: 6 أجهزة ← تقدير **3 أشخاص** (نفر واحد عنده موبايل وإيربود وساعة)
- النتيجة بتتقارن مع حساب الـ ESP نفسه وتأخذ الأعلى

---

## 4. 📈 `peak_forecast_model_1st.joblib`

| | |
|---|---|
| **الاسم** | نموذج التنبؤ بأوقات الذروة |
| **النوع** | XGBoost Classifier |
| **المدخلات** | `hour` + `minute` + `day_of_week` (الوقت الحالي فقط!) |

**بيعمل إيه؟**

مدرّب على جدول جامعة محاكى لأسبوع كامل، بيعرف متى الطلاب بيكونوا موجودين:

> **"الساعة دي في اليوم ده — المتوقع إيه؟"**

| Output | المعنى |
|---|---|
| `0 = idle` | فترة هدوء (مثلاً 3 الصبح) |
| `1 = medium` | حركة متوسطة (وقت استراحة) |
| `2 = peak` | ذروة (بين المحاضرات) |

> **مثال:** لو الساعة 3 الصبح وفي حد متقاط في المكان، النظام يعتبره **شذوذ (Anomaly)** وينبّه فوراً!

---

## 🔄 كيف يعملون معاً؟

```
البيزو يُضغط عليه
        ↓
[1] piezo_step_model ← خطوة حقيقية؟
        ↓ نعم
الطاقة تتولد وتُسجَّل ✅

━━━━━━━━━━━━━━━━━━━━━━━━

الـ ESP يلتقط إشارات WiFi
        ↓
[2] wifi_occupancy_model ← في ناس؟
        ↓ نعم
[3] wifi_count_model ← كام واحد؟
        ↓
[4] peak_forecast_model ← المتوقع كام في الوقت ده؟
        ↓
لو الفرق كبير → 🚨 تنبيه شذوذ!
```

---

## 📁 مكان الملفات

كل النماذج موجودة في مجلد:
```
data cleaning and AI models/
├── piezo_step_model.joblib
├── wifi_occupancy_model.joblib
├── wifi_count_model.joblib
└── peak_forecast_model_1st.joblib
```

وكل بيانات التدريب والسكريبتات اللي اتبنت بيها النماذج موجودة في نفس المجلد.

---

*PowerStep Grid — AI-Powered Floor System © 2026*
